#!/bin/bash

usage() {
cat << EOF
Usage: submit_apps [http://127.0.0.1:5000/events]

Retrieves all events available from the given end point
EOF
exit -1
}

if [[ -z $1 ]]; then
  usage
fi

curl -GET ${1}
